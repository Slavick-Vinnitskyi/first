//binary TREE
#include <iostream>
#include <stdio.h>
#include <string.h>
#include <iomanip>

using namespace std;

struct TREE
{ char inf;
TREE *left, *right;
};

int main()
{
    TREE *curr = new TREE;
/*бинарное дерево поиска
    сurrent = root;  починаємо пошук з кореня дерева
    stop = 0;
    while (сurrent != NULL && stop == 0)
    if (x < сurrent ->info) сurrent = сurrent ->left;
    else if (x > current ->info) сurrent = сurrent ->right;
    else stop = 1;
*/
}